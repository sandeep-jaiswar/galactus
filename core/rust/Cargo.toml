[package]
name = "galactus-core"
version = "0.1.0"
edition = "2021"
authors = ["Galactus Team"]
description = "Galactus production inference engine - deterministic capital pressure inference"
license = "TBD"

# Production inference engine
# All dependencies must be justified and deterministic

[dependencies]
# Core functionality (to be added as needed after promotion)
chrono = { version = "0.4", features = ["serde"] }  # Date/time handling for events
ordered-float = "4.0"  # Ordered floating point comparisons

serde = { version = "1.0", features = ["derive"] }  # Serialization for data structures (enable when needed)
serde_json = "1.0"  # JSON serialization for persistence
tokio = { version = "1", features = ["full"] }      # Async runtime
# polars = "0.35"                                      # Data processing

# API dependencies
tonic = "0.12"  # gRPC framework
prost = "0.13"  # Protocol buffers
warp = "0.3"    # HTTP web framework

# Infrastructure dependencies
uuid = { version = "1.0", features = ["v4", "serde"] }  # UUID generation for intent IDs
async-trait = "0.1"  # Async trait support for data sources

[dev-dependencies]
# Testing utilities
# criterion = "0.5"  # Benchmarking
tempfile = "3.8"  # Temporary files for testing persistence

[lib]
name = "galactus_core"
path = "src/lib.rs"

# Production build profile - optimize for correctness, then speed
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = 'abort'

# Note: All dependencies require justification in comments
# See: docs/02-system-architecture/rust-python-boundary-enforcement.md
