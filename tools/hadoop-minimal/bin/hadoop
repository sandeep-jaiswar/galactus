#!/bin/sh
# Minimal hadoop wrapper to satisfy Hive scripts in local dev
if [ "$#" -eq 0 ] || [ "$1" = "version" ]; then
  echo "Hadoop 2.7.3"
  echo "Subversion https://example.com -r 0"
  exit 0
fi
# emulate other hadoop commands as no-op
exit 0
