apiVersion: v1
kind: Secret
metadata:
  name: clickhouse-credentials
  namespace: galactus
type: Opaque
stringData:
  # IMPORTANT: Replace these values with actual credentials before applying
  # Never commit actual secrets to version control
  username: "galactus_user"
  password: "CHANGE_ME_TO_STRONG_PASSWORD"
---
apiVersion: v1
kind: Secret
metadata:
  name: galactus-secrets
  namespace: galactus
type: Opaque
stringData:
  # Add any other sensitive configuration here
  # Example: API keys, tokens, etc.
  clickhouse-password: "CHANGE_ME_TO_STRONG_PASSWORD"
